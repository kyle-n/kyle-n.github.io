import { c as create_ssr_component } from "./ssr.js";
const metadata = {
  "layout": "post",
  "title": "How Node.js Fakes Multithreading",
  "date": "2023-02-15T00:00:00.000Z",
  "keywords": "javascript, node",
  "image": "reactor-pattern.png",
  "caption": "From Node.js Design Patterns by Mario Casciaro and Luciano Mammino"
};
const How_node_js_fakes_multithreading = create_ssr_component(($$result, $$props, $$bindings, slots) => {
  return `<p data-svelte-h="svelte-18hdyb5"><a href="https://www.bitovi.com/backend-consulting/nodejs-consulting" rel="nofollow">Node.js</a> is one of the most popular languages for web servers, even though <a href="https://codeburst.io/is-javascript-single-threaded-youre-kidding-me-80b11d74f4e5" rel="nofollow">JavaScript is single-threaded</a>. This might seem contradictory at first---don’t web servers need to handle simultaneous connections?</p> <p data-svelte-h="svelte-l2nwos">The answer is something built into the language called <strong>the reactor pattern</strong>. The reactor pattern allows Node.js to not only “multithread” but to do so in a resource-efficient way that makes it <a href="https://www.bitovi.com/blog/5-reasons-to-choose-nodejs" rel="nofollow">perfect for microservices</a>.</p> <p data-svelte-h="svelte-1d1crme">This post is based on and uses diagrams from <a href="https://www.nodejsdesignpatterns.com/" rel="nofollow">Node.js Design Patterns</a> by Mario Casciaro and Luciano Mammino. It’s an excellent book I would recommend to anyone interested in Node.</p> <h2 data-svelte-h="svelte-15qj6v8">A Brief History of Web Servers</h2> <p data-svelte-h="svelte-kuozup">To understand Node.js, let’s look at what web servers have to do.</p> <p data-svelte-h="svelte-zfixdf">Imagine you are running a simple REST API for sandwiches. The user can <code>POST</code>, <code>GET</code>, <code>PATCH</code>, or <code>DELETE</code> sandwiches from a server-side database through an API.</p> <p data-svelte-h="svelte-4b2l4c">Here’s what happens when a user sends a GET request:</p> <ol data-svelte-h="svelte-iugtve"><li>The server accepts the incoming request</li> <li>It establishes a TCP connection</li> <li>It reads request content from the socket</li> <li>It retrieves the requested sandwich data from the database</li> <li>It writes the response to the client</li> <li>It closes the connection</li></ol> <p data-svelte-h="svelte-1vdu0pu">These steps are not true of <em>all</em> server connections---you might use <code>keep-alive</code>headers to <a href="https://connectreport.com/blog/tuning-http-keep-alive-in-node-js/" rel="nofollow">reuse the previous TCP connection</a>, or you might be using WebSockets, or you might not be using REST at all. But to explain a GET request to a RESTful JSON API, this model will suffice.</p> <p data-svelte-h="svelte-yml6q9">One major problem facing web servers is the time it takes to read the database. Slow read times can be caused by a few things: database size, instance distribution, etc.</p> <p data-svelte-h="svelte-n70br7">Simultaneous connections compound the database read-time problem. Most websites are used by more than one person at a time. Your web server cannot just hang around waiting for the response from the database before responding to the next user! Imagine waiting in line to use Google.</p> <p data-svelte-h="svelte-1puzcwa">Servers have handled this problem in several different ways over the years. The first web server, <a href="https://www.w3.org/Daemon/" rel="nofollow">httpd</a>, would spin up a whole new process for each connection to it. This avoided multithreaded code, which is hard to write correctly, and weird race conditions. However, httpd consumed a <em>lot</em> of memory, and it hasn’t been updated since 1997.</p> <p data-svelte-h="svelte-w34pct">So people moved on. These days, many server frameworks use a <a href="https://en.wikipedia.org/wiki/Thread_pool" rel="nofollow">thread pool</a>. Many requests come into a server, which assigns one thread to each request. Then, if the thread is waiting on a slow database request, it’s no big deal to other users. Their fast request can complete on some other thread.</p> <p data-svelte-h="svelte-a451pm">This is a good, flexible approach to scaling. Just add more threads! The downside is it can also take up a lot of memory. Consider this diagram of a server that uses one thread per connection.</p> <p data-svelte-h="svelte-1mlxqcn"><img src="/static/img/multithreading-1.png" alt="A diagram showing multiple threads handling multiple connections to a server"></p> <h2 data-svelte-h="svelte-1i79bmg">The Node.js Approach</h2> <p data-svelte-h="svelte-z5jta5">Node.js is written in JavaScript. It is single-threaded, meaning it can only do one thing at a time.</p> <p data-svelte-h="svelte-1jhtc6j">At first glance, this seems incompatible with serving multiple connections simultaneously. However, Node.js and JavaScript fake multithreading using the <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/EventLoop" rel="nofollow">event loop</a> to hop between multiple tasks.</p> <p data-svelte-h="svelte-q32pz9">To return to your sandwich API, let’s say the server uses this <a href="https://expressjs.com/" rel="nofollow">Express</a> code to respond to GET requests:</p> <pre class="language-javascript"><!-- HTML_TAG_START -->${`<code class="language-javascript">router<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span><span class="token string">"/sandwiches/:id"</span><span class="token punctuation">,</span> <span class="token punctuation">(</span><span class="token parameter">reg<span class="token punctuation">,</span> res</span><span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
  <span class="token keyword">const</span> sandwichId <span class="token operator">=</span> req<span class="token punctuation">.</span>params<span class="token punctuation">.</span>id<span class="token punctuation">;</span>
  database<span class="token punctuation">.</span><span class="token function">findOneById</span><span class="token punctuation">(</span>sandwichId<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">then</span><span class="token punctuation">(</span><span class="token parameter">sandwich</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
    console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">"lunch time"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    res<span class="token punctuation">.</span><span class="token function">json</span><span class="token punctuation">(</span>sandwich<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">"searching for sandwich"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code>`}<!-- HTML_TAG_END --></pre> <p data-svelte-h="svelte-1aeb8wr">When this code runs, it will execute these steps:</p> <ol data-svelte-h="svelte-18ba5ug"><li>Assign <code>sandwichId</code></li> <li>Dispatch an asynchronous operation, <code>findOneById</code></li> <li>Log <code>&quot;searching for sandwich&quot;</code></li> <li>Wait for the database to return the requested sandwich</li> <li>Run the callback provided on line 3 when the database returns some data</li> <li>Log <code>&quot;lunch time&quot;</code></li> <li>Send the sandwich data to the client</li> <li>Wait for a new request</li></ol> <p data-svelte-h="svelte-17ovwr3">Node.js can fake multithreading and multiple clients because, during steps 4 and 8, <strong>it can serve other requests</strong>. If another request comes in during step 2, Node can respond to it during step 4, then go back and continue at step 5 to finish responding to the original request.</p> <p data-svelte-h="svelte-1cmmre2">Think of Node as a chef running around the kitchen. The chef can set a pot of water to boil, then jump over and chop some onions, then put dry spaghetti in the water, then chop some carrots. The chef can only do one thing at a time, but they can <strong>multitask</strong>.</p> <div style="width:100%;height:0;padding-bottom:56%;position:relative;" data-svelte-h="svelte-4lrr8j"><iframe src="https://giphy.com/embed/UovJw8VQReFiiIy8C9" width="100%" height="100%" style="position:absolute" frameborder="0" class="giphy-embed" allowfullscreen></iframe></div><p data-svelte-h="svelte-1kdsabc"><a href="https://giphy.com/gifs/team17-overcooked-2-overcooked2-UovJw8VQReFiiIy8C9">via GIPHY</a></p> <p data-svelte-h="svelte-ydfsj0">Node.js fakes multithreading by multitasking. It spreads out tasks, not across threads or processes, but <strong>time</strong>.</p> <p data-svelte-h="svelte-jgkt7k"><img src="/static/img/multithreading-2.png" alt="A diagram showing a Node server handling multiple connections on one thread over time"></p> <p data-svelte-h="svelte-p2zt5i">The way Node hops around between tasks is explained by the reactor pattern. Node is structured like a reactor, where the flow of control swirls around the event loop, processing tasks as they come in.</p> <p data-svelte-h="svelte-1bpi87n"><img src="/static/img/reactor-pattern.png" alt="A diagram demonstrating the reactor pattern in Node"></p> <p data-svelte-h="svelte-1k70se6">When you run a JavaScript file in Node, it starts at the top square, “Execute Handler.” It will synchronously run the code provided until that code is finished. If an asynchronous operation is triggered while running the code, that operation is sent to the box on the left, the <strong>event demultiplexer</strong>.</p> <p data-svelte-h="svelte-28cso2">The synchronous event demultiplexer is a complicated name for a part of Node that just means “in-progress async operations.” When your JS code triggers a request to the file system or network, the synchronous event demultiplexer is where the request lives in Node while it’s in progress. If Node is not actively running JS code, Node will “block” or wait here until one of the async operations finishes.</p> <p data-svelte-h="svelte-19iqfaq">When an async operation in the demultiplexer finishes, Node moves it to the event queue. When Node is finished running the current bit of JavaScript, it gets the next finished async operation from the queue and runs the callback associated with it.</p> <p data-svelte-h="svelte-28b9le">Let’s return to your sandwich API one more time:</p> <pre class="language-javascript"><!-- HTML_TAG_START -->${`<code class="language-javascript">router<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span><span class="token string">"/sandwiches/:id"</span><span class="token punctuation">,</span> <span class="token punctuation">(</span><span class="token parameter">reg<span class="token punctuation">,</span> res</span><span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
  <span class="token keyword">const</span> sandwichId <span class="token operator">=</span> req<span class="token punctuation">.</span>params<span class="token punctuation">.</span>id<span class="token punctuation">;</span>
  database<span class="token punctuation">.</span><span class="token function">findOneById</span><span class="token punctuation">(</span>sandwichId<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">then</span><span class="token punctuation">(</span><span class="token parameter">sandwich</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
    console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">"lunch time"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    res<span class="token punctuation">.</span><span class="token function">json</span><span class="token punctuation">(</span>sandwich<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">"searching for sandwich"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code>`}<!-- HTML_TAG_END --></pre> <p data-svelte-h="svelte-1823syp">This can now be explained using the reactor pattern. First, the application synchronously runs the code in the <code>.get()</code> callback. It adds an async event to the event demultiplexer (the database request). Then, Node won’t do anything because it has no more JS to run.</p> <p data-svelte-h="svelte-1s0vvw6">When the database request finishes, the event demultiplexer adds it to the event queue. Node grabs it from the queue and runs the callback associated with the finished event. In this case, it’s the anonymous function on lines 3 - 6. When the callback is finished, Node waits for another request.</p> <p data-svelte-h="svelte-pntjlz">Node runs on a single piece of JavaScript until it’s finished. Any async operations are added to the demultiplexer. When the operations finish, they’re added to the event queue. When Node is done running the current piece of JS, it grabs the next item from the queue. If that item causes another async operation, that operation is added to the demultiplexer, and so on.</p> <h2 data-svelte-h="svelte-mnlqo9">How Does Node.js “Multithreading” Scale?</h2> <p data-svelte-h="svelte-1k6bdb0">It’s understandable to question how Node.js scales once you understand it only runs on one thread. How can this serve when you have many users?</p> <p data-svelte-h="svelte-1x6qffn">First, even if JavaScript is not incredibly efficient, JavaScript interpreters are. Google has put significant money and development time into V8, the JS engine that powers Node. Apple has done the same with JavaScriptCore, the interpreter that runs in Safari (and all web content on iOS). ARM chips <a href="https://stackoverflow.com/questions/50966676/why-do-arm-chips-have-an-instruction-with-javascript-in-the-name-fjcvtzs" rel="nofollow">ship with special hardware capabilities to handle JS math</a>.</p> <p data-svelte-h="svelte-1k92xp6">Second, scaling servers is actually a <a href="https://www.quora.com/How-does-Node-js-handle-multiple-connections" rel="nofollow">whole other problem</a> that exists outside of your choice of server language. If you need industrial scale for your web app, you’ll have to worry about load balancing and proxies and CDNs and lots of other things before the user even touches your server API code.</p> <h4 data-svelte-h="svelte-109yxv">Works Cited</h4> <ul data-svelte-h="svelte-zzce8s"><li><a href="https://www.nodejsdesignpatterns.com/" rel="nofollow">Node.js Design Patterns</a> by Mario Casciaro and Luciano Mammino (3rd Ed.)</li> <li><a href="https://tianpan.co/blog/2015-01-13-understanding-reactor-pattern-for-highly-scalable-i-o-bound-web-server" rel="nofollow">Understanding Reactor Pattern for Highly Scalable I/O Bound Web Server</a> by Tian Pan</li></ul>`;
});
export {
  How_node_js_fakes_multithreading as default,
  metadata
};
